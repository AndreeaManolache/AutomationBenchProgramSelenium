package steps;
import org.openqa.selenium.WebDriver;

public class StepsInitialization {
   // protected ConfigFileReader configFileReader = ConfigFileReader.getInstance();

    public WebDriver driver;
}
